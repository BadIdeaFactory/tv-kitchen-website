---
title: Press
---

import { graphql } from 'gatsby'
import PressTpl from '@ui/templates/PressTpl'

export default PressTpl

export const pageQuery = graphql`
  query {
    allMediumPost {
      edges {
        node {
          id
          title
          previewContent2 {
            bodyModel {
              paragraphs {
                text
              }
            }
          }
          firstPublishedAt
          uniqueSlug
        }
      }
    }
  }
`

<!-- No content edits allowed as all data gets fetched from Medium.com -->